"use client";

import { Button } from "mtxuilib/ui/button";

// import { Button } from "mtxuilib/ui/button";
export default function Page() {
  return (
    <>
      <Button
        // intent="secondary"
        type="button"
        onClick={() => {
          // router.push(coreConfig.oauthAuthorizeUrl);
        }}
      >
        aa
      </Button>
    </>
  );
}
