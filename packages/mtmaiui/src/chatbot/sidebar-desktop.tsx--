"use client";
import { Sidebar } from "./sidebar";

export function SidebarDesktop() {
	// const user = useUserInfo();

	return (
		<Sidebar className="peer absolute inset-y-0 z-20 hidden mt-14 -translate-x-full border-r bg-muted duration-300 ease-in-out data-[state=open]:translate-x-0 lg:flex lg:w-[250px] xl:w-[300px]">
			{/* <ChatHistory userId={user?.data?.id?.toString()} /> */}
		</Sidebar>
	);
}
