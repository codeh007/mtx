"use client";
import dynamic from "next/dynamic";

export const AppLazy = dynamic(
  () =>
    import(
      /* webpackChunkName: "dashapp" */
      "../App"
    ).then((x) => x.App),
  {
    ssr: false,
  },
);
