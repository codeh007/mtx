'use client'

/**
 * 演示 使用另外一个独立的react 前端组件,mount 到当前的 dom 中
 * @returns 
 */
export default function Page() {

    const loaderScripSrc = "https://unpkg.com/react@18/umd/react.development.js"
    return <div>hello intergrate</div>
}
